<nav class="navbar navbar-expand-md">
    <div class="nav-container">
        <div class="navbar-brand me-lg-5" href="/">
            <a href="/" class="text-center fw-semibold">Employee Review System</a>
        </div>

        <div class="d-flex btn-div">
            <% if(locals.user) {%>

                <div class="text-center fw-semibold">
                    Hello <%= user.username %>!
                </div>
                <a class="btn btn-danger mx-2 l fw-semibold" href="/sign-out">
                    Sign Out</a>

                <%} %>
        </div>
    </div>
</nav>